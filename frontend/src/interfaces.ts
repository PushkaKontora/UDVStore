export interface IUser {
    username: string; // email;
    password: string;
    id: number;
    first_name: string;
    last_name: string;
    patronymic: string;
    balance: number;
    image?: string;
}
//
//
// --proxy-config proxy.config.json
